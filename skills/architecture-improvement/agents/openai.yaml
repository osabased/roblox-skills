interface:
  display_name: "Architecture Improvement"
  short_description: "Make one justified architecture improvement"
  default_prompt: "Use $architecture-improvement to evaluate one bounded codebase scope. Delegate large reconnaissance to capability-matched explorers and probes, make one justified improvement autonomously when safe, and return NO CHANGE when the Intervention Gate fails."
policy:
  allow_implicit_invocation: false
