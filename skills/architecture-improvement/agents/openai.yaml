interface:
  display_name: "Architecture Improvement"
  short_description: "Make one justified architecture improvement"
  default_prompt: "Use $architecture-improvement to decide whether one bounded architecture change is justified, delegate large-codebase reconnaissance to capability-matched explorers and probes, implement autonomously when safe, and stop with NO CHANGE when intervention is unsupported."
policy:
  allow_implicit_invocation: false
