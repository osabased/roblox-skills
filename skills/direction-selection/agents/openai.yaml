interface:
  display_name: "Direction Selection"
  short_description: "Choose among genuinely competing directions"
  default_prompt: "Use $direction-selection only when a material direction choice is genuinely live. Route it proportionately, avoid routine or cheaply reversible choices, and return the local decision plus Direction Gate status."
