# Required dependency for the validator scripts in scripts/.
#
# PyYAML is required, not optional: the previous bundled fallback parser could
# disagree with PyYAML (empty scalars, octal-like integers, duplicate keys,
# tabs), which let the same registry/store/record file pass validation in one
# environment and fail in another. Trust verdicts must not depend on which
# parser happens to be installed. Scripts exit with code 2 and an install hint
# when PyYAML is missing. Everything else is Python 3.10+ standard library.
PyYAML>=5.4
