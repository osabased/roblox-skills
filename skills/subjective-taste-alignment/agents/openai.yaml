interface:
  display_name: "Subjective Taste Alignment"
  short_description: "Align consequential preference-sensitive choices"
  default_prompt: "Use $subjective-taste-alignment when a consequential subjective choice is unresolved. Use ephemeral alignment for task-local evidence and profile-backed alignment only when reusable state is actually needed; authorize propagation before material downstream work."
